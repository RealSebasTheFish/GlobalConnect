# GlobalConnect Backend

Backend documentation has been consolidated into the repository root README.

Please use:

- `../README.md`

That file includes:

- current feature coverage across frontend and backend
- manual Eclipse import and startup flow (no startup scripts in this version)
- service run order and port mapping
- hosted deployment link
- latest Postman v4 import and execution breakdown
