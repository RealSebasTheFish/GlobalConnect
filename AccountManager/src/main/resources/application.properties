server.port=8081

# creates the SQLite DB file beside where you run the app (project/workspace root)
spring.datasource.url=jdbc:sqlite:accountmanager.db
spring.datasource.driver-class-name=org.sqlite.JDBC

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql